# OWASP ZAP Configuration File
# Used for baseline scans

# Exclude Unity WebGL endpoints from active scanning
# These endpoints serve binary data and should not be actively scanned
excludeFromScan=.*\.(data|wasm|gz)$

# Scan policy - reduce aggressiveness for Unity WebGL apps
# Focus on React app endpoints, not Unity binary files

